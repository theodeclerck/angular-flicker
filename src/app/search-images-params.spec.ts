import { SearchImagesParams } from './search-images-params';

describe('SearchImagesParams', () => {
  it('should create an instance', () => {
    expect(new SearchImagesParams()).toBeTruthy();
  });
});
